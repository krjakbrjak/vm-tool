# Setting Up VM Networking on Linux

This repo contains scripts that help create bridges, TAP devices, and start QEMU VM instances using these interfaces.

## Usage

```bash
cd scripts
./bridge.sh
./tap.sh
./start.sh -c ./cloud
```
